# PickPic 真机验证清单

自动化测试不能覆盖系统相册权限、真实媒体库行为和原生视频全屏。发布前请在至少一台 Android 和一台 iPhone 上逐项验证，并记录设备型号、系统版本、应用版本和复现步骤。

## Android

- [ ] 首次启动拒绝照片/视频权限：页面不崩溃，列表为空，扫描不会启动。
- [ ] 仅允许选择部分照片：只显示已授权照片；扫描、删除和收藏不会触碰未授权媒体。
- [ ] 从系统设置撤销权限后回到应用：队列和列表及时清空，不会继续显示旧媒体。
- [ ] 授予完整权限后重新进入：媒体库重新加载，之前合法的队列项目可继续操作。
- [ ] 删除一张和批量删除多张照片：系统确认次数符合预期；删除失败时队列保留，可重试。
- [ ] 删除视频：回收站、恢复和最终删除状态一致，应用重启后状态不漂移。
- [ ] 扫描开始、暂停/停止、恢复、重置：按钮状态与实际扫描一致；扫描完成后重复组和人脸结果可读取。
- [ ] 快速切换页面或后台/前台：扫描和媒体加载不因旧请求回写错误状态；无卡死或重复弹窗。
- [ ] 视频 Feed：自动播放/暂停、静音、分享、删除和快速滑动均正常。
- [ ] 视频长按全屏：画面连续播放，关闭按钮和系统返回都能退出；切换 Tab 后全屏窗口不会残留。
- [ ] Android 14/15：确认照片选择器、通知/存储相关系统行为与目标 SDK 兼容。

## iOS

- [ ] 首次启动拒绝照片权限：页面不崩溃，列表为空，扫描不会启动。
- [ ] 选择“限制访问”：只显示选中的照片；请求相册筛选或管理相册时给出需要完整权限的明确提示。
- [ ] 在系统设置中使用“编辑所选照片”增加/移除媒体：返回应用后列表和队列刷新，不出现已撤销项目。
- [ ] 从限制访问升级为完整访问，再降回限制访问：权限范围与 UI 状态一致，不会把限制状态当成完整权限。
- [ ] 删除一张和批量删除多张照片/视频：删除失败时队列保留，可重试；成功后本地扫描索引同步清理。
- [ ] 扫描开始、停止、恢复、重置，以及扫描进行中切换 Tab/后台：状态、进度和最终结果一致。
- [ ] 视频 Feed：自动播放/暂停、静音、分享、删除和快速滑动均正常。
- [ ] 视频长按进入原生全屏、系统退出全屏、切换 Tab：播放器和页面状态一致，无残留遮罩。
- [ ] iOS 版本升级后：检查相册权限弹窗文案、限制访问入口和相册筛选行为没有改变。

## 证据与问题记录

- [ ] 保存每个平台至少一段关键流程录屏，避免包含无关个人媒体内容。
- [ ] 记录 `device model / OS version / app version / permission scope / selected albums`。
- [ ] Android 同步保存 `adb logcat` 相关片段；iOS 保存 Xcode 控制台相关片段。
- [ ] 记录操作前后媒体数量、队列数量、扫描进度和是否重启过应用。
- [ ] 报告问题前移除真实照片、文件路径、资产 ID 等个人信息。
